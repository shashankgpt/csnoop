import { CompareFieldValidatorDirective } from './compare-field-validator.directive';

describe('CompareFieldValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new CompareFieldValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
